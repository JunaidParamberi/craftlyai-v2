/**
 * Standard spacing for forms wrapped in `Card` + `CardContent` + `CardFooter`
 * (space above the footer divider and the actions row).
 */
export const FORM_CARD_CONTENT_BEFORE_FOOTER = "pb-8";

/** CRM-style footer: Cancel / Save aligned end (e.g. client + project forms). */
export const FORM_CARD_FOOTER_END_ACTIONS =
  "flex flex-wrap justify-end gap-2 border-t border-border pt-8 pb-2";

/** Onboarding-style footer: back link + stacked actions, full width. */
export const FORM_CARD_FOOTER_ONBOARDING_SPLIT =
  "shrink-0 flex flex-wrap items-center justify-between gap-4 border-t border-border bg-card px-6 pt-8 pb-8";
