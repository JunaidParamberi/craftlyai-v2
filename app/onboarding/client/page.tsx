import { CompleteOnboardingPanel } from "@/components/onboarding/complete-onboarding-panel";

export const metadata = {
  title: "First client",
};

export default function OnboardingClientPage() {
  return <CompleteOnboardingPanel />;
}
